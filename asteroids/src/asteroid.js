require('./moving_object.js');
require('./util.js');

function Asteroid (pos) {
    this.pos = pos;
    this.vel = 
    this.color = "white";
    this.radius = 25;
    
    MovingObject.call(this, options);
}

